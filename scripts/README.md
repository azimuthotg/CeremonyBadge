# Extract Images from Excel - คู่มือการใช้งาน

สคริปต์สำหรับดึงรูปภาพจาก Excel และตั้งชื่อไฟล์ตามชื่อ-สกุลของบุคคล

## 📋 สารบัญ
- [การติดตั้ง](#การติดตั้ง)
- [วิธีใช้งาน](#วิธีใช้งาน)
- [เพิ่มไฟล์ Excel ใหม่](#เพิ่มไฟล์-excel-ใหม่)
- [ปัญหาที่พบบ่อย](#ปัญหาที่พบบ่อย)

## 📦 การติดตั้ง

สคริปต์นี้ต้องการ Python packages:
- openpyxl
- pandas
- Pillow

ติดตั้งผ่าน requirements.txt:
```bash
source Ceremony_env/bin/activate
pip install -r requirements.txt
```

## 🚀 วิธีใช้งาน

### วิธีที่ 1: Extract ไฟล์เดียว (แนะนำ)

```bash
source Ceremony_env/bin/activate
python scripts/extract_single_file.py
```

โปรแกรมจะถาม:
1. ชื่อไฟล์ Excel (หรือเลือกจากรายการ)
2. ชื่อ Sheet ที่ต้องการ extract
3. ยืนยันการทำงาน

### วิธีที่ 2: Extract ทุกไฟล์ในโฟลเดอร์

```bash
source Ceremony_env/bin/activate
python scripts/extract_all_files.py
```

จะ extract รูปจากทุกไฟล์ .xlsx ใน `media/files/card_original/`

### วิธีที่ 3: ผ่าน Command Line (สำหรับผู้ชำนาญ)

```bash
source Ceremony_env/bin/activate
python scripts/extract_images_from_excel.py \
  --file "media/files/card_original/ชื่อไฟล์.xlsx" \
  --sheet "ชื่อ sheet" \
  --output "media/photos/extracted"
```

## ➕ เพิ่มไฟล์ Excel ใหม่

### ขั้นตอน:

1. **วางไฟล์ Excel ในโฟลเดอร์**
   ```
   media/files/card_original/
   └── ชื่อหน่วยงานใหม่.xlsx
   ```

2. **รันสคริปต์**
   ```bash
   source Ceremony_env/bin/activate
   python scripts/extract_single_file.py
   ```

3. **เลือกไฟล์และ Sheet**
   - สคริปต์จะแสดงรายการไฟล์ทั้งหมด
   - เลือกไฟล์ที่ต้องการ
   - เลือก sheet ที่มีรูป

4. **ตรวจสอบผลลัพธ์**
   ```
   media/photos/extracted/
   └── ชื่อหน่วงานใหม่/
       └── ชื่อsheet/
           ├── ชื่อคน1.png
           ├── ชื่อคน2.png
           └── ...
   ```

## 📝 โครงสร้าง Excel ที่รองรับ

สคริปต์รองรับ 2 รูปแบบ:

### รูปแบบที่ 1: คอลัมน์รวม
```
| ลำดับ | ยศ - ชื่อ สกุล | บัตรประชาชน | ... | รูปภาพ |
```

### รูปแบบที่ 2: คอลัมน์แยก
```
| ลำดับ | ยศ | ชื่อ | นามสกุล | ... | รูปภาพ |
```

**หมายเหตุ:**
- Header ต้องอยู่ที่ **แถว 3** (row index 2)
- รูปภาพต้องอยู่ในแถวเดียวกับข้อมูล
- คอลัมน์ต้องมีคำว่า "ชื่อ" อยู่ในชื่อคอลัมน์

## 🔧 การปรับแต่ง

### เปลี่ยนตำแหน่ง Header

แก้ไขใน `extract_images_from_excel.py`:
```python
header_row = 2  # เปลี่ยนเป็น 0, 1, 2, ... ตามต้องการ
```

### เพิ่มยศที่ต้องการตัดออก

แก้ไขฟังก์ชัน `extract_name_only()`:
```python
titles = [
    'นางสาว', 'นาย', 'นาง',
    # เพิ่มยศใหม่ตรงนี้
    'ร.ต.', 'ร.ท.', 'ร.อ.',
    ...
]
```

### เปลี่ยนโฟลเดอร์ output

```bash
python scripts/extract_single_file.py --output "media/photos/ชื่อโฟลเดอร์ใหม่"
```

## ⚙️ คุณสมบัติพิเศษ

### ✅ ตัดยศอัตโนมัติ
- นาย, นาง, นางสาว
- ยศทหาร: พ.อ., พ.ท., ร.ต., ฯลฯ
- ยศตำรวจ: ด.ต., พ.ต.อ., น.สพ., ฯลฯ

### ✅ จัดการไฟล์ซ้ำ
ถ้าชื่อซ้ำจะเพิ่มหมายเลขท้าย:
```
สมชาย ใจดี.png
สมชาย ใจดี_1.png
สมชาย ใจดี_2.png
```

### ✅ ข้ามแถวว่าง
ถ้าแถวไม่มีข้อมูลชื่อ จะข้ามไม่บันทึกรูป

### ✅ รองรับ 2 รูปแบบ
- คอลัมน์รวม: "ยศ - ชื่อ สกุล"
- คอลัมน์แยก: "ยศ", "ชื่อ", "นามสกุล"

## 🐛 ปัญหาที่พบบ่อย

### Q: รูปที่ extract ออกมายืดข้าง?
**A:** รูปถูก extract ตามต้นฉบับที่ฝังใน Excel ถ้ารูปต้นฉบับยืดอยู่แล้ว ก็จะได้รูปยืด (ไม่ได้แก้ไขอะไร)

### Q: บางรูปไม่ถูก extract?
**A:** เช็คว่า:
- แถวนั้นมีข้อมูลชื่อไหม?
- รูปอยู่ในตำแหน่งที่ถูกต้องไหม?
- Header row ตั้งค่าถูกต้องไหม?

### Q: ชื่อไฟล์ยังมียศอยู่?
**A:** ยศนั้นอาจยังไม่ได้เพิ่มในลิสต์ `titles` ให้เพิ่มเข้าไปในฟังก์ชัน `extract_name_only()`

### Q: ต้องการ extract เฉพาะบาง sheet?
**A:** ใช้ `extract_single_file.py` แล้วเลือก sheet ที่ต้องการ

## 📞 ติดต่อสอบถาม

หากพบปัญหาหรือต้องการเพิ่มฟีเจอร์:
1. เปิด issue ใน GitHub repository
2. ติดต่อผู้ดูแลระบบ

## 📄 ไฟล์ที่เกี่ยวข้อง

```
scripts/
├── README.md                          # เอกสารนี้
├── extract_images_from_excel.py       # สคริปต์หลัก
├── extract_single_file.py             # Extract ไฟล์เดียว (แนะนำ)
└── extract_all_files.py               # Extract ทุกไฟล์
```

## 🎯 ตัวอย่างการใช้งาน

### ตัวอย่าง 1: Extract ไฟล์ใหม่

```bash
# 1. วางไฟล์ใน media/files/card_original/
# 2. รันสคริปต์
source Ceremony_env/bin/activate
python scripts/extract_single_file.py

# 3. เลือกไฟล์และ sheet ตามที่โปรแกรมถาม
# 4. รอจนเสร็จ
# 5. รูปจะอยู่ใน media/photos/extracted/
```

### ตัวอย่าง 2: Extract ทุกไฟล์ที่เหลือ

```bash
source Ceremony_env/bin/activate
python scripts/extract_all_files.py

# จะ extract ทุกไฟล์ที่ยังไม่เคยทำ
```

---

**สร้างเมื่อ:** 2025-12-14
**Version:** 1.0
**โปรเจค:** NPU CeremonyBadge System
