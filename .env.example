# =============================================================================
# NPU CeremonyBadge System - Environment Configuration
# =============================================================================
# คัดลอกไฟล์นี้เป็น .env และกรอกค่าจริงเข้าไป
# Copy this file to .env and fill in the actual values
#
# สำคัญ: ห้ามเปิดเผยหรือ commit ไฟล์ .env เข้า git
# Important: Never expose or commit the .env file to git
# =============================================================================

# -----------------------------------------------------------------------------
# Django Core Settings
# -----------------------------------------------------------------------------

# Secret Key (ต้องสร้างใหม่สำหรับ production)
# Generate new key: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here-change-this-in-production

# Debug Mode (ตั้งเป็น False ใน production)
# Set to False in production
DEBUG=True

# Allowed Hosts (โดเมนหรือ IP ที่อนุญาต คั่นด้วย comma)
# Allowed domains or IPs, separated by comma
# Example: example.com,www.example.com,192.168.1.100
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------------------------------------------------------
# Database Configuration (MySQL)
# -----------------------------------------------------------------------------

# Database Engine (ใช้ค่าเดิมได้)
DB_ENGINE=django.db.backends.mysql

# Database Name (ชื่อฐานข้อมูล)
DB_NAME=CeremonyBadge

# Database User (username สำหรับเข้าถึงฐานข้อมูล)
DB_USER=your_database_user

# Database Password (รหัสผ่านฐานข้อมูล - ระวังอย่าเปิดเผย!)
DB_PASSWORD=your_database_password

# Database Host (ที่อยู่ server ฐานข้อมูล)
# localhost สำหรับเครื่องเดียวกัน
# หรือ IP address/domain สำหรับ remote database
DB_HOST=localhost

# Database Port (พอร์ตฐานข้อมูล MySQL default: 3306)
DB_PORT=3306

# -----------------------------------------------------------------------------
# Notes / หมายเหตุ
# -----------------------------------------------------------------------------
#
# 1. สร้างไฟล์ .env โดยคัดลอกจาก .env.example:
#    cp .env.example .env
#
# 2. แก้ไขค่าในไฟล์ .env ตามสภาพแวดล้อมจริง
#
# 3. สำหรับ production:
#    - ตั้ง DEBUG=False
#    - สร้าง SECRET_KEY ใหม่
#    - กำหนด ALLOWED_HOSTS ให้ถูกต้อง
#    - ใช้รหัสผ่านฐานข้อมูลที่แข็งแกร่ง
#
# 4. ไฟล์ .env จะถูกละเว้นโดย .gitignore อัตโนมัติ
#
# =============================================================================
