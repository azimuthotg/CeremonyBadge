# 📋 คู่มือระบบ Import ข้อมูลจาก Excel

## 🎯 ภาพรวม

ระบบ Import จาก Excel ช่วยให้ Submitter สามารถนำเข้าข้อมูลบุคลากรจำนวนมากพร้อมกันได้ โดยไม่ต้องกรอกทีละคน

**วันที่พัฒนา:** 9 พฤศจิกายน 2568

---

## ✅ ฟีเจอร์ที่พัฒนาเสร็จแล้ว

### 1. **Parse Excel Function** (`apps/registry/utils.py:148-226`)
- อ่านไฟล์ Excel (.xlsx)
- ข้ามแถว header 4 แถว เริ่มอ่านจากแถวที่ 5
- รองรับข้อมูลครบถ้วน รวม:
  - ข้อมูลพื้นฐาน (ยศ, ชื่อ, นามสกุล, บัตรประชาชน, หน่วยงาน, หน้าที่, อายุ)
  - การรับวัคซีนทั้ง 4 เข็ม
  - การตรวจโควิด (RT-PCR, ATK, วัดอุณหภูมิ)

### 2. **Form** (`apps/registry/forms.py:254-339`)
- `ExcelImportForm` สำหรับอัปโหลดไฟล์
- Dropdown เลือก Badge Type (required) และ Zone (optional - สามารถเลือก "ไม่เลือก" เพื่ออัพเดตทีหลัง)
- Validation:
  - ไฟล์ต้องเป็น .xlsx
  - ขนาดไม่เกิน 10MB

### 3. **Views** (`apps/registry/views.py:876-1112`)
- `staff_import_upload` - อัปโหลดและ parse ไฟล์
- `staff_import_preview` - แสดง preview พร้อมเช็คข้อมูลซ้ำ
- `staff_import_confirm` - บันทึกลงฐานข้อมูล
- `staff_import_success` - แสดงผลลัพธ์

### 4. **URL Patterns** (`apps/registry/urls.py:38-42`)
```python
/registry/import/upload/    → หน้าอัปโหลด
/registry/import/preview/   → หน้า preview
/registry/import/confirm/   → บันทึกข้อมูล (POST)
/registry/import/success/   → แสดงผลลัพธ์
```

### 5. **Templates**
- `templates/registry/import/staff_import_upload.html` - หน้าอัปโหลด
- `templates/registry/import/staff_import_preview.html` - หน้า preview ข้อมูล
- `templates/registry/import/staff_import_success.html` - หน้าแสดงผลลัพธ์

### 6. **ปุ่ม Import** (`templates/registry/staff_list.html:13-14`)
- เพิ่มปุ่มสีเขียว "นำเข้าจาก Excel" ที่หน้ารายการบุคลากร

---

## 📊 โครงสร้างไฟล์ Excel

### Headers (แถวที่ 1-4)
- **แถว 1:** หัวข้อหลัก (เช่น "บัญชีรายชื่อเจ้าหน้าที่ปฏิบัติหน้าที่ในงาน บัตรชมพู")
- **แถว 2:** พื้นที่/โซน (เช่น "พื้นที่ ม.นครพนม (I)")
- **แถว 3:** หัวคอลัมน์หลัก (ยศ, ชื่อ, การรับวัคซีน, การตรวจโควิด ฯลฯ)
- **แถว 4:** หัวคอลัมน์ย่อย (เข็ม 1, 2, 3, 4, RT-PCR, ATK, วัดอุณหภูมิ)
- **แถว 5+:** ข้อมูลจริง

### คอลัมน์ (A-P)

| คอลัมน์ | ชื่อฟิลด์ | ประเภท | ตัวอย่าง |
|---------|-----------|--------|----------|
| A | ลำดับ | Integer | 1, 2, 3... |
| B | ยศ | String | นาย, นาง, พล.อ., ศ.ดร. |
| C | ชื่อ | String | สมชาย |
| D | นามสกุล | String | ใจดี |
| E | บัตรประชาชน | String (13 หลัก) | 1234567890123 |
| F | หน่วยงาน | String | ม.นครพนม |
| G | ประเภทบุคคล | String | บุคคลชั้นในสุด |
| H | หน้าที่ | String | เจ้าหน้าที่ตรวจสอบ |
| I | อายุ | Integer | 35 |
| J | วัคซีนเข็ม 1 | Boolean (1/0) | 1 = รับวัคซีน |
| K | วัคซีนเข็ม 2 | Boolean (1/0) | 1 = รับวัคซีน |
| L | วัคซีนเข็ม 3 | Boolean (1/0) | 1 = รับวัคซีน |
| M | วัคซีนเข็ม 4 | Boolean (1/0) | 1 = รับวัคซีน |
| N | RT-PCR | Boolean (1/0) | 1 = ตรวจแล้ว |
| O | ATK | Boolean (1/0) | 1 = ตรวจแล้ว |
| P | วัดอุณหภูมิ | Boolean (1/0) | 1 = ตรวจแล้ว |
| Q | รูปภาพ | - | (ว่างไว้) |

---

## 🔄 Workflow การ Import

```
1. Upload File
   ↓
2. Parse Excel + Validate
   ↓
3. Store in Session
   ↓
4. Show Preview Table
   ├── ✅ พร้อม import
   └── ⚠️  ข้อมูลซ้ำ (เช็คจากบัตรประชาชน)
   ↓
5. User Confirm
   ↓
6. Insert to Database
   ├── Create StaffProfile
   ├── Create BadgeRequest (status='draft')
   └── Skip ข้อมูลซ้ำ
   ↓
7. Show Success Summary
   ├── ✅ นำเข้าสำเร็จ X รายการ
   ├── ⚠️  ข้ามซ้ำ Y รายการ
   └── ❌ Error Z รายการ
```

---

## 🎨 หน้าจอหลัก

### 1. **Upload Page** (`/registry/import/upload/`)
- ฟอร์มอัปโหลดไฟล์ Excel
- Dropdown เลือก Badge Type (บัตรชมพู, แดง, เหลือง, เขียว) - **บังคับเลือก**
- Dropdown เลือก Zone (A, B, C) - **ไม่บังคับ** สามารถเลือก "ไม่เลือก" เพื่ออัพเดตภายหลัง
- คำแนะนำรูปแบบไฟล์

### 2. **Preview Page** (`/registry/import/preview/`)
- สรุปข้อมูล: ไฟล์, ประเภทบัตร, โซน (หรือ "ไม่ระบุ" ถ้าไม่เลือก)
- สถิติ: ทั้งหมด, พร้อม import, ข้อมูลซ้ำ
- ตารางแสดงข้อมูลทั้งหมด รวมคอลัมน์: ลำดับ, ยศ, ชื่อ-นามสกุล, บัตรประชาชน, **ประเภทบุคคล**, หน้าที่, อายุ, วัคซีน, ตรวจโควิด, สถานะ
- ไฮไลต์ข้อมูลซ้ำด้วยสีเหลือง
- แสดงวัคซีน 4 เข็ม (เขียว=รับแล้ว, เทา=ไม่รับ) และการตรวจโควิด (ฟ้า=ตรวจแล้ว)
- สถานะแสดงเป็นไอคอน (✅=พร้อม, ⚠️=ซ้ำ)
- **ปุ่มยืนยัน → เปิด Bootstrap Modal** แทน alert confirm

### 3. **Success Page** (`/registry/import/success/`)
- แสดงผลลัพธ์: สำเร็จ, ข้าม, error
- รายละเอียด error (ถ้ามี)
- คำแนะนำขั้นตอนต่อไป
- ลิงก์กลับหน้ารายการหรือ import เพิ่ม

---

## ⚠️ การตรวจสอบข้อมูลซ้ำ

ระบบเช็คข้อมูลซ้ำจาก **บัตรประชาชน 13 หลัก**

```python
# ใน staff_import_preview view
existing = StaffProfile.objects.filter(national_id=national_id).first()
if existing:
    item['is_duplicate'] = True
    item['duplicate_info'] = f'ซ้ำกับ: {existing.full_name}'
```

**การจัดการข้อมูลซ้ำ:**
- หน้า Preview: แสดงสีเหลือง + ข้อความแจ้งเตือน
- หน้า Confirm: ข้ามรายการซ้ำอัตโนมัติ
- หน้า Success: แจ้งจำนวนที่ข้าม

---

## 📝 หมายเหตุสำคัญ

### 1. **รูปภาพ**
- ❌ ไม่สามารถ import รูปภาพจาก Excel ได้
- ✅ ต้องแนบรูปภาพภายหลังผ่านการแก้ไขข้อมูล (wizard step 2)

### 2. **สถานะเริ่มต้น**
- ข้อมูลที่ import เข้ามาจะเป็นสถานะ `draft`
- ต้องแนบรูปภาพก่อนจึงจะส่งคำขอได้

### 3. **หน่วยงาน**
- ระบบจะพยายามหาหน่วยงานจากชื่อในคอลัมน์ F
- ถ้าไม่พบ จะใช้หน่วยงานของผู้ import แทน

### 4. **Transaction Safety**
- ใช้ `transaction.atomic()` เพื่อความปลอดภัย
- ถ้า import ผิดพลาดจะ rollback ทั้งหมด

---

## 🧪 การทดสอบ

### Test Case 1: Import ข้อมูลใหม่ทั้งหมด
1. อัปโหลดไฟล์ Excel ที่ไม่มีข้อมูลซ้ำ
2. ตรวจสอบ Preview ว่าทุกรายการเป็นสีเขียว (พร้อม)
3. Confirm import
4. ตรวจสอบว่าข้อมูลเข้าฐานข้อมูลครบถ้วน

### Test Case 2: Import ข้อมูลซ้ำบางส่วน
1. อัปโหลดไฟล์ที่มีบัตรประชาชนซ้ำกับในระบบ
2. ตรวจสอบ Preview ว่าข้อมูลซ้ำเป็นสีเหลือง
3. Confirm import
4. ตรวจสอบว่าข้อมูลซ้ำถูกข้าม

### Test Case 3: ไฟล์ผิดรูปแบบ
1. อัปโหลดไฟล์ .xls หรือ .csv
2. ตรวจสอบว่าแสดง error message
3. ไม่สามารถ submit ได้

---

## 🔧 Dependencies ที่เพิ่มเติม

```txt
openpyxl==3.1.5   # สำหรับอ่านไฟล์ Excel .xlsx
pandas==2.3.3     # สำหรับจัดการข้อมูล (ติดตั้งไว้เผื่อใช้ในอนาคต)
```

**ติดตั้ง:**
```bash
source Ceremony_env/bin/activate
pip install openpyxl pandas
```

หรือ

```bash
pip install -r requirements.txt
```

---

## 📂 ไฟล์ที่เกี่ยวข้อง

### Backend
1. `apps/registry/utils.py:148-226` - parse_excel_staff()
2. `apps/registry/forms.py:254-339` - ExcelImportForm
3. `apps/registry/views.py:876-1112` - 4 import views
4. `apps/registry/urls.py:38-42` - URL patterns

### Frontend
1. `templates/registry/import/staff_import_upload.html`
2. `templates/registry/import/staff_import_preview.html`
3. `templates/registry/import/staff_import_success.html`
4. `templates/registry/staff_list.html:13-14` - ปุ่ม import

### Configuration
1. `requirements.txt:10-11` - openpyxl, pandas

---

## 🚀 การใช้งาน

### สำหรับ Submitter:
1. เข้าหน้า "รายการบุคลากร" → คลิกปุ่ม **"นำเข้าจาก Excel"**
2. เลือกไฟล์ Excel พร้อมระบุ Badge Type และ Zone
3. ตรวจสอบข้อมูลในหน้า Preview
4. คลิก **"ยืนยันนำเข้าข้อมูล"**
5. ดูผลลัพธ์และกลับไปแนบรูปภาพในแต่ละรายการ

### สำหรับ Admin:
- สามารถเข้าถึงได้เหมือน Submitter
- มีสิทธิ์นำเข้าข้อมูลทุก Badge Type และ Zone

---

## 🔮 การพัฒนาในอนาคต (แนะนำ)

1. **Export Template Excel** - ให้ดาวน์โหลด template ว่างเปล่าได้
2. **Validation ละเอียดขึ้น** - เช็ครูปแบบบัตรประชาชน 13 หลัก
3. **Import รูปภาพ** - รองรับ import รูปภาพจาก URL หรือ Base64
4. **Progress Bar** - แสดง progress ขณะ import ข้อมูลจำนวนมาก
5. **Export รายงาน** - Export ข้อมูลปัจจุบันเป็น Excel
6. **Undo Import** - ลบข้อมูลที่ import ไปทั้งชุด

---

**พัฒนาโดย:** Claude Code AI
**วันที่:** 9 พฤศจิกายน 2568
**เวอร์ชัน:** 1.0
